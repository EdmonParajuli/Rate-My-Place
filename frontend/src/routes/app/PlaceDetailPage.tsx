import { Store } from "lucide-react"
import { ScreenPlaceholder } from "./ScreenPlaceholder"

// Place Detail is its own not-yet-built screen ticket - this stub exists so
// PlaceCard's "View Place" link goes somewhere honest instead of nowhere.
export function PlaceDetailPage() {
  return <ScreenPlaceholder icon={Store} label="Place detail" />
}
