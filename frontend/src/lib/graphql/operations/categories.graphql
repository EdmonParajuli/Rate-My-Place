query Categories {
  categories {
    data {
      id
      label
      icon
      coverImageUrl
      businessCount
      avgRating
    }
  }
}

query Category($id: Int!) {
  category(id: $id) {
    data {
      id
      label
      icon
      coverImageUrl
      businessCount
      avgRating
    }
  }
}
